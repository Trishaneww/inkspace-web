export default function ClientRequestsPage() {
  return (
    <div>
      <h1>My Requests</h1>
      {/*
        List of tattoo requests the client has submitted.
        Each shows: status, matched artists count, description summary.
        CTA to submit a new request → /request
      */}
    </div>
  );
}
