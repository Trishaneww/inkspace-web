export default function ArtistSettingsPage() {
  return (
    <div>
      <h1>Artist Settings</h1>
      {/*
        — Profile info (bio, styles, location, socials)
        — Booking page customization (slug, description, banner)
        — Availability windows
        — Deposit & pricing rules
        — Lead preferences (styles to receive, budget range)
        — Notification preferences
        — Payment / payout settings (Stripe Connect)
      */}
    </div>
  );
}
