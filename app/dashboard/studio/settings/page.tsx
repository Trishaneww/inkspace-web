export default function StudioSettingsPage() {
  return (
    <div>
      <h1>Studio Settings</h1>
      {/*
        — Studio profile (name, location, logo, bio)
        — Artist roster management (invite / remove artists)
        — Booking rules and deposit policies
        — Payout settings
      */}
    </div>
  );
}
