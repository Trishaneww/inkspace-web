export const StudioSidebar = () => {
  return (
    <aside>
      {/*
        Nav items:
        — Overview         /dashboard/studio
        — Artists          /dashboard/studio/artists
        — Bookings         /dashboard/studio/bookings
        — Settings         /dashboard/studio/settings
      */}
    </aside>
  );
}
