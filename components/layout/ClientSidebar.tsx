export const ClientSidebar = () => {
  return (
    <aside>
      {/*
        Nav items:
        — My Dashboard     /dashboard/client
        — My Requests      /dashboard/client/requests
        — Artist Matches   /dashboard/client/matches
        — My Inquiries     /dashboard/client/inquiries
        — My Appointments  /dashboard/client/bookings
        — Settings         /dashboard/client/settings
        ——
        — Browse Artists   /artists
        — Flash Market     /flash
      */}
    </aside>
  );
}
